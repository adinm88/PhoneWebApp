# PhoneWebApp
A simple web app that has the capability to connect to a SQL database to read and write different contacts into it. Also demonstrates simple routing techniques with the nav bar as well.

Must also supply the ConnectionString in appsettings.json for the app to work!!!!
